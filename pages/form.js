import Layout from "../components/layout";
import { Text } from "@geist-ui/react";

const Form = () => {
  return (
    <Layout>
      <div>
        <h2>Form</h2>
      </div>
    </Layout>
  );
};

export default Form;
